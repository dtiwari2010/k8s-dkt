apiVersion: v1
kind: pod
metadata:
  name: app-pod
    leblel:
       app: myapp
       type: front-end
spec:
  containers:
    - name : nginx-dkt-container
      image: nginx

    - name: backend-container
      image: radis